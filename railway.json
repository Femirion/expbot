{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "./gradlew clean bootJar"
  },
  "deploy": {
    "startCommand": "java -Xmx980m -XX:+ExitOnOutOfMemoryError -jar build/libs/*.jar"
  }
}
